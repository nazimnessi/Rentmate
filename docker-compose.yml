version: '3.4'

services:
  web:
    # depends_on:
    #   - db
    build: .
    ports: 
      - "8000:8000"
    image: rentmate_web:latest
    restart: always
    command: bash -c 
        "python manage.py migrate
        && python manage.py runserver 0.0.0.0:8000"
    # environment:
    #   - REDIS_HOST=redis
    volumes:
      - .:/rentmate
    tty: true
